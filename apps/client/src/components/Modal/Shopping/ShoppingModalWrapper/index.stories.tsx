import ShoppingModalWrapper from '.';

import type { Meta, StoryObj } from '@storybook/react';

const meta: Meta<typeof ShoppingModalWrapper> = {
  component: ShoppingModalWrapper,
};

export default meta;

type Story = StoryObj<typeof ShoppingModalWrapper>;

export const Primary: Story = {};
